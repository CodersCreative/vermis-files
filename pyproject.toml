[project]
name = "vermis"
version = "0.0.1"
dependencies = [
  "customtkinter",
  "opencv-python",
  "pillow",
  "ultralytics",
  "gpiozero",
  "inputs"
]
extend-exclude = [
    "*.pyc",
    "__pycache__",
]
