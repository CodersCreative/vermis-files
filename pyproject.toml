[project]
name = "vermis"
version = "0.0.1"
dependencies = [
  "opencv-python",
  "pillow",
  "ultralytics",
  "gpiozero",
  "inputs",
  "nicegui",
  "fastapi"
]
extend-exclude = [
    "*.pyc",
    "__pycache__",
]
